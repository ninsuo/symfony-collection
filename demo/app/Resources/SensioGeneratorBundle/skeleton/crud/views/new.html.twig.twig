{% block extends %}
{{ "{% extends 'FuzQuickStartBundle::layout.html.twig' %}" }}
{% endblock extends %}

{% block body %}
{{ "{% block body -%}" }}
    <h1>{{ "{{ 'quickstart.crud.new.title' | trans({'%entity%': '" ~ entity ~ "'}) }}" }}</h1>

    {{ '{{ form(form) }}' }}

    {% set hide_edit, hide_delete = true, true %}
    {% include 'crud/views/others/record_actions.html.twig.twig' %}
{{ "{% endblock %}" }}
{% endblock body %}
