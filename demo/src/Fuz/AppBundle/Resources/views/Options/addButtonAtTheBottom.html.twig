{% extends 'FuzAppBundle::layout.html.twig' %}

{% block extra_js %}
    <script src="{{ asset('bundles/fuzapp/js/collection/jquery.collection.js') }}"></script>
{% endblock %}

{% block title %}JavaScript options : change the "add" button's default location{% endblock %}

{% block body %}

    <h2>{{ block('title') }}</h2>

    <p>Use <code>add_at_the_end</code> option set to <code>false</code> (default) to create an <code>add</code> for each element of the collection.</p>

    <p>Use <code>add_at_the_end</code> option set to <code>true</code> if you want to have only one <code>add</code> button, located below the collection.</p>

    <p>
        Check out this page's
        <a href="https://github.com/ninsuo/symfony2-collection/blob/master/demo/src/Fuz/AppBundle/Controller/OptionsController.php" target="_blank">controller</a>,
        and
        <a href="https://github.com/ninsuo/symfony2-collection/blob/master/demo/src/Fuz/AppBundle/Resources/views/Options/addButtonAtTheBottom.html.twig" target="_blank">view</a>
        to know how to create such form and enable the plugin.
    </p>

    <br/>

    <div class="row">

        <div class="col-md-6">
            <h3>With the option set to <code>false</code></h3>

            <p>
                This is the default behaviour, an <code>add</code> button is added for each element of the collection.
            </p>

            {% form_theme formDisabled 'FuzAppBundle:Options:options-theme.html.twig' %}

            {{ form(formDisabled) }}

            {% for value in formDisabledData.values %}
                <p>Value : {{ value }}</p>
            {% endfor %}

        </div>
        <div class="col-md-6">
            <h3>With the option set to <code>true</code></h3>

            <p>
                Only one <code>add</code> button is available, located at the bottom of the collection.
            </p>

            {% form_theme formEnabled 'FuzAppBundle:Options:options-theme.html.twig' %}

            {{ form(formEnabled) }}

            {% for value in formEnabledData.values %}
                <p>Value : {{ value }}</p>
            {% endfor %}

        </div>

    </div>

    <hr/>

{% endblock %}

{% block script %}

    <script type="text/javascript">

        $('.formDisabled-collection').collection();

        $('.formEnabled-collection').collection({
            add_at_the_end: true,
        });

    </script>

{% endblock %}