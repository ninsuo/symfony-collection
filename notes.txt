
-- don't forget to set the right tag name in package.json

git push origin master
git tag -a '2.1.5' -m 'tag'
git push origin 2.1.5
bower register symfony-collection git://github.com/ninsuo/symfony-collection.git
npm publish
